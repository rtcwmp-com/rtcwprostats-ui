# prostats web client

## Getting started

1. Clone this repository
2. Run `yarn` in repository folder
3. Run `yarn dev` to start a local development server

## Misc

`yarn ts` will run typescript checks

`yarn build` will run typescript checks and build for production
